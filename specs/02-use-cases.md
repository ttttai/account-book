# ユースケースと受け入れ条件

状態: 承認済み

バージョン: 0.2.28

## UC-001 グループを作成する

操作者: 認証済みユーザー

基本フロー:

1. ユーザーがグループ名を入力する。
2. アプリがJPY、Asia/Tokyo、選択された週の開始曜日、標準負担方法を設定する。
3. グループとowner所属情報を原子的に作成する。
4. 初期カテゴリをグループへ複製する。
5. 作成したグループを選択中にする。

受け入れ条件:

- `AC-GRP-001-1` 未認証の要求を拒否する。
- `AC-GRP-001-2` 正常な要求で、グループ1件とowner所属1件だけを作成する。
- `AC-GRP-001-3` 途中失敗しても、ownerのいないグループを残さない。
- `AC-GRP-001-4` 作成者は直後に新規グループのカレンダーを開ける。
- `AC-GRP-001-5` 別ユーザーは、IDを推測してもグループへアクセスできない。
- `AC-CAT-001-1` 作成したグループに、承認済みの支出7件・収入3件の初期カテゴリを重複なく作成する。

## UC-002 メンバーを招待・追加する

操作者: owner/adminが招待を作成し、認証済みユーザーが承認する。

基本フロー:

1. ownerまたはadminが、追加後の権限を`admin`または`member`から選んで招待を作成する。
2. アプリは暗号学的乱数による256 bit以上のURL-safeな生トークンを生成し、SHA-256 hashだけをDBへ保存する。
3. 生トークンをURL fragmentへ含めた共有リンクを作成結果に1度だけ表示する。有効期限はサーバー時刻から72時間とする。
4. 招待された利用者がリンクを開く。未認証の場合は、同一tabの`sessionStorage`に一時保持してGoogleログイン後に承認画面へ戻る。
5. Server Actionが生トークンの形式と認証を検証し、hashを使ってDB commandを実行する。
6. DB commandが招待行をlockし、有効性と許可済みGoogle identityを再確認して、所属作成または再有効化と招待使用済み更新を原子的にcommitする。
7. 承認後は生トークンを`sessionStorage`から削除し、参加したグループへ遷移する。

受け入れ条件:

- `AC-GRP-004-1` 招待権限のないmemberは招待を作成できない。
- `AC-GRP-004-2` 生トークンは作成時に1度だけ返し、DBにはハッシュだけを保存する。
- `AC-GRP-004-3` 期限切れ、取消済み、使用済みの招待を拒否する。
- `AC-GRP-004-4` 同一ユーザーによる同一招待の再承認は冪等に処理する。
- `AC-GRP-004-5` 承認によって所属情報を重複作成しない。
- `AC-GRP-004-6` 招待リンクはアプリからメール送信せず作成者に1度だけ表示し、許可リスト外のGoogleアカウントは有効なトークンを持っていても承認できない。
- `AC-GRP-004-7` 生トークンは256 bit以上のURL-safeな暗号学的乱数とし、DBにはSHA-256 hashだけを保存する。
- `AC-GRP-004-8` 共有リンクは生トークンをURL fragmentへ格納し、request path、query、cookie、log、永続storageへ含めない。未認証からのログイン継続に使う場合も同一tabの`sessionStorage`だけへ一時保持し、承認成功時に削除する。
- `AC-GRP-004-9` owner/adminだけが保留中招待を一覧・取消でき、招待で`owner`を指定できない。取消の再送は安全に同じ結果を返す。
- `AC-GRP-004-10` 未使用招待を既存のアクティブメンバーが承認した場合は所属を増やさず招待を使用済みにする。削除済みの同一所属がある場合は同じ所属IDを招待roleで再有効化し、過去参照を維持する。

## UC-003 共有支出を登録する

例: メンバーAが食費6,000円を支払い、AとBで均等負担する。

期待結果:

```text
グループ支出: 6,000円
Aの支払額: 6,000円
Aの支出: 3,000円
Bの支払額: 0円
Bの支出: 3,000円
```

受け入れ条件:

- `AC-TXN-001-1` 金額は正の安全な整数でなければならない。
- `AC-TXN-001-2` 支払者と負担メンバーは、同じグループのアクティブメンバーでなければならない。
- `AC-TXN-001-3` 負担額合計は6,000円と一致する。
- `AC-TXN-001-4` 取引と負担額を原子的に保存する。
- `AC-TXN-001-5` 操作者は認証済みユーザーになる。
- `AC-TXN-001-6` カレンダーのグループ合計が6,000円だけ増える。
- `AC-TXN-001-7` AとBの支出がそれぞれ3,000円増える。
- `AC-TXN-001-8` 同じ`client_request_id`を再送した場合、既存結果を返す。
- `AC-TXN-001-9` スマートフォンでは支出カテゴリの名称を常時表示し、選択肢全体を44 x 44 CSS pixel以上のタップ領域として選択できる。選択状態は色だけに依存せず、キーボードでも操作できる。
- `AC-TXN-001-10` 負担方法（画面では「分け方」）は1人、均等、カスタムの順に表示し、初期選択は1人とする。均等へ切り替えた場合は全アクティブメンバーを負担者に選択する。
- `AC-TXN-018-1` 支出の登録・編集フォームに支払者の入力欄を表示せず、登録時は現在のメンバー、編集時は保存済みの支払者（削除済みメンバーなら現在のメンバー）を隠しfieldで送信する。Server ActionとDB関数の支払者検証は変更しない。
- `AC-TXN-018-2` 支出フォームの文言は「分け方」「支出した人」「内訳の確認」とし、「負担」「支払者」「支払った人」を表示しない。最後に使った支払者を端末へ記憶しない。
- `AC-TXN-018-3` 履歴の各行、日別取引sheet、固定費の一覧・フォームに支出の支払者を表示せず、内訳は「内訳」と表記する。収入の受取者表示は維持する。
- `AC-TXN-018-4` 詳細分析のメンバー別表は「支出額」「受取額」の2列とし、支払額の列を表示しない。集計純関数とDTOは変更しない。
- `AC-TXN-014-1` 金額欄はOSの仮想キーボードを開かず、画面内テンキーの1〜9、0、00で入力し、1文字削除できる。
- `AC-TXN-014-2` 物理キーボードとスクリーンリーダーからも金額を入力・確認できる。
- `AC-TXN-014-3` 金額の入力中も、カテゴリ選択と保存操作を画面内へ表示し続ける。
- `AC-TXN-014-5` テンキーは金額欄へfocusしたときに開き、金額欄以外の入力欄へfocusしたときに閉じる。入力ドック内（カテゴリ・キー・保存）の操作では開閉状態を変えない。
- `AC-TXN-014-6` テンキーを閉じている間も金額欄から再び開けることを画面上の説明で示し、物理キーボードとスクリーンリーダーからの金額入力は開閉状態にかかわらず維持する。
- `AC-TXN-014-7` テンキーを開いたとき、入力中の金額欄が入力ドックへ隠れない位置へ表示する。
- `AC-TXN-014-4` 金額は先頭に0を作らず、安全な整数の上限を超える桁を追加しない。上限や不正な入力は既存の値を壊さない。
- `AC-TXN-014-8` テンキーには「閉じる」キーを置き、押すとテンキーを閉じて再開の説明（`AC-TXN-014-6`）を表示する。入力済みの金額・式と他の入力値を失わず、フォームを送信しない。閉じた直後のfocusは金額欄へ置き、この移動ではテンキーを開かない。以後は金額欄のタップまたは再focusで開く。
- `AC-TXN-014-9` 「閉じる」は入力ドックの右上（「カテゴリ」見出し行の右端、「すべて」の真上）へ置き、44 x 44 CSS pixel以上のタップ領域を持つ。テンキーの行を追加せず、数字キーの配列（00と2列幅の0）を変えない。ドックの高さの増加は見出し行の範囲にとどめる。320pxと375pxで「すべて」・カテゴリ行・数字キーと重ならず欠けない。カテゴリ展開中はテンキーと一緒に隠し、900px以上（入力ドックを固定しない表示）では「すべて」と同様に表示しない。固定費と予算のテンキーは変更しない。
- `AC-TXN-014-10` 金額欄は入力中も金額を3桁区切りで表示し（`128000`と打つと`128,000`）、式では左辺・右辺の各項を区切って「= ¥1,500」の結果表示と表記をそろえる（`1,200+300`）。送信する金額と式の状態は区切りなしの整数のままとし、物理キーボードや貼り付けで入った`,`は無視する。区切りはlocale実装に依存しない決定的な整形で行う。文字を小さくする8文字以上の判定は区切り記号を含めた表示文字数で行う。固定費と予算の金額欄の表示は`AC-REC-005-5`・`AC-BUD-010-5`で同じ区切り規則に従う。
- `AC-TXN-015-1` カテゴリは既定で1行に表示し、選択中のカテゴリを常に判別できる。
- `AC-TXN-015-2` 「すべて」を押すと全カテゴリを2列で展開し、キーボードとスクリーンリーダーから到達できる。スワイプだけでしか展開できない操作にしない。
- `AC-TXN-015-3` 展開中にカテゴリを選ぶと1行表示へ戻り、選択したカテゴリを保持する。
- `AC-TXN-016-1` 保存操作は入力ドック内へ常設し、テンキーの開閉とカテゴリの展開のどの状態でも画面から消えず、ドックの高さを増やさない。テンキーを開いている間は数字キーの右列、閉じている間は全幅で表示する。
- `AC-TXN-016-2` 保存中は保存操作を無効化し、二重送信を防ぐ。
- `AC-TXN-015-4` 展開・折りたたみでフォームの他の入力値を失わない。
- `AC-TXN-017-1` テンキーの÷、×、−、+で金額欄に「左辺 演算子 右辺」の式を組み立てられる。式に含める演算子は1つまでとし、右辺を入力した状態で別の演算子を押すと左辺と右辺を計算して結果を新しい左辺にする。金額欄が空のときに演算子を押しても何も起きず、右辺が空の間に演算子を押すと演算子を置き換える。物理キーボードの`+`、`-`、`*`、`/`も同じ演算子として受け付ける。
- `AC-TXN-017-2` =を押すと式を計算し、金額欄を計算結果だけの表示へ戻す。結果が0円のときは空欄へ戻す。右辺が空の式では左辺だけを残す。
- `AC-TXN-017-3` 式を入力している間は、保存時に使う計算結果を金額欄の近くへ「= ¥1,500」の形で即時表示する。送信する金額は常に計算結果とし、=を押さずに保存しても計算結果を保存する。右辺が空の式は左辺を保存する。
- `AC-TXN-017-4` 計算は整数のJPYで行い、浮動小数点を使わない。÷の端数は四捨五入する。結果が0円未満になる式と安全な整数の上限を超える式は計算せず、理由を金額欄の近くへ表示し、送信する金額を空にする。演算子や=を押しても式は壊さない。右辺は先頭0を禁止するため0だけの右辺を作れず、0で割る式は組み立てられない（計算関数は外部入力に備えて0除算も拒否する）。
- `AC-TXN-017-5` 右辺にも先頭0の禁止と安全な整数の上限（`AC-TXN-014-4`）を適用する。1文字削除は末尾の数字または演算子を1文字ずつ取り消す。
- `AC-TXN-017-6` 演算子キーと=は44 x 44 CSS pixel以上のタップ領域を持ち、追加によって入力ドックの高さを増やさない。保存は数字キーの右列に常設したまま、320pxと375pxで数字・演算子・1文字削除・=・保存が重ならず欠けない。固定費と予算のテンキーは変更しない。

均等割りの端数規則: JPYの整数へ分割し、余りはmembership IDの昇順で1円ずつ加算する。送信前に計算結果を画面表示する。

支出登録の入力規則:

- 金額は文字列として受け取り、`1`からJavaScriptの安全な整数上限`9,007,199,254,740,991`円までの10進整数だけを許可する。桁区切り、小数、指数表記、符号付き表記は受け付けない。
- 取引日は実在する`YYYY-MM-DD`だけを許可し、初期値はグループのタイムゾーンにおける当日とする。将来日を一律には拒否しない。
- メモは前後の空白を除去して500文字以内とし、空文字は未設定として保存する。
- 「均等」は選択した1人以上のアクティブメンバーへ配分し、「1人」は選択した1人へ全額を配分し、「カスタム」は正の整数で入力した1人以上の負担額を使う。0円の負担行は保存しない。
- 画面で計算した負担額は確認表示に使うが、Server ActionとDB commandは選択メンバー、合計、グループ所属を独立して再検証する。
- 支出登録画面を開いた時点で`client_request_id`を生成し、結果不明の再送では同じ値を使う。成功後に同じ値が再送された場合は既存取引IDを返し、重複登録しない。

## UC-004 グループ内の個人支出を登録する

例: メンバーAが1,200円を支払い、全額をAへ割り当てる。

- `AC-TXN-002-1` グループ支出が1,200円増える。
- `AC-TXN-002-2` Aの支出が1,200円増える。
- `AC-TXN-002-3` Bの支出は変わらない。
- `AC-TXN-002-4` 非公開取引はMVP対象外のため、両方のアクティブメンバーから閲覧できる。

## UC-005 取引を編集する

- `AC-TXN-008-1` 更新には現在のversionが必要である。
- `AC-TXN-008-2` 古いversionによる更新は競合として返し、新しいデータを上書きしない。
- `AC-TXN-008-3` 金額と負担額を一緒に検証する。
- `AC-TXN-008-4` 認証済み操作者と更新日時を記録する。
- `AC-TXN-008-5` カレンダーと履歴に同じ更新結果を表示する。

## UC-006 取引を削除する

- `AC-TXN-009-1` 削除は取引本体と負担行を同じtransactionで物理削除する。
- `AC-TXN-009-2` 削除した取引をカレンダー・履歴・CSVの合計から即時に除外する。
- `AC-TXN-009-3` 未削除の取引への削除は現在のversionと一致する場合だけ実行し、古いversionは競合として拒否する。
- `AC-TXN-009-4` すでに削除済み（存在しない）取引への削除の再送は、状態を変更せず成功として扱う。
- `AC-TXN-009-5` 削除操作と確認dialogは、固定入力ドックと画面下部ナビゲーションに覆われない位置へ表示し、幅320px・375px・1280pxで最下部までscrollすればタップできる。

削除の取り消し・復元機能は提供しない。誤削除への備えは、手動DB dump（NFR-REC-003）とCSV出力を利用者側の運用とする。

## UC-007 月間カレンダーを表示する

- `AC-CAL-001-1` 初期月はグループのタイムゾーン上の当月とする。
- `AC-CAL-001-2` `?month=YYYY-MM`で別の月を選択できる。
- `AC-CAL-001-3` 不正な月指定では未処理例外を出さず、安全な検証エラー状態を表示する。
- `AC-CAL-001-4` グループ対象では各支出を1度だけ数える。
- `AC-CAL-001-5` メンバー対象では、選択メンバーの負担額だけを合計する。
- `AC-CAL-001-6` 削除済み取引と収入を支出合計から除外する。
- `AC-CAL-001-7` 選択月以外の日付を月間合計へ含めない。
- `AC-CAL-001-8` 非メンバーへグループデータを返さない。
- `AC-CAL-001-9` `scope=group`は取引金額、`scope=self`と`scope=member`は対象membershipの負担額を日別・月別に合計する。
- `AC-CAL-001-10` `scope=member`では同じグループのアクティブmembership IDを必須とし、不正・削除済み・別グループのIDへデータを返さない。
- `AC-CAL-001-11` 対象メンバーの月間集計は負担額を「支出」として統一し、支払額を集計領域へ表示しない。
- `AC-CAL-001-12` カレンダーはグループの週開始曜日に従う42セルとし、選択月外のセルへ金額を表示しない。
- `AC-CAL-001-13` `day=YYYY-MM-DD`が選択月内の実在日なら、同日の支出を作成日時の新しい順で表示する。
- `AC-CAL-001-14` 不正な`scope`、`member`、`day`では未処理例外を出さず、取引データを含まない検証エラー状態を表示する。
- `AC-CAL-001-15` 日別・月別合計はJPY安全整数として計算し、浮動小数点による金額計算を行わない。
- `AC-CAL-001-16` 0円でない日別合計は、金額の大小にかかわらず`10,000`のような桁区切り10進数字で正確に表示し、`万`表記への省略や丸めを行わない。
- `AC-CAL-001-17` 同じ月・scope・member内の日付選択と解除は、追加のserver navigationやDB queryを発生させず即時に日別パネルだけを更新する。`day`はURLへ同期し、再読み込み・共有・ブラウザの戻る／進むでも同じ有効な選択を復元する。
- `AC-CAL-001-18` 日別取引sheetの各取引行（単発の支出・収入、固定費の展開行）は、カテゴリ名とともにカテゴリに設定された色を補助情報として表示する。色は許可済みの色tokenだけを`data-*`属性とCSSで描画し、DBやDTOの色文字列をinline styleやclass名へ組み込まない。色が未定義・許可外でもカテゴリ名を省略せず、既定色で崩れなく表示する。
- `AC-CAL-001-19` 月移動は前月・翌月ボタンと「今日」操作だけで行い、カレンダー本体上の横方向のpointer操作（タッチのスワイプ、マウスのドラッグ）では月を移動しない。横方向のpointer操作は日付セルのタップ、縦スクロール、`AC-CAL-001-17`の即時反映を妨げない。
- `AC-CAL-001-20` ホームカレンダーの6行は、`100dvh`からheader・月移動・集計対象・月間合計・曜日見出し・下部ナビゲーション前の余白を除いた残り高さを均等に分け合い、行高に固定の上限を設けない。375 x 812では表の下端から下部ナビゲーション上端までの空きを32px以内とし、余った高さがある間は各行の高さの差を2px以内に保つ。行の下限は44pxとし、下限でも収まらない高さでは欠落・重なりの代わりに縦スクロールを許す。日付セルの金額は表の幅に応じて拡大し、375px幅で11px以上、1280 x 800で16px以上とし、320px幅でも`+999,999`を1行で欠落なく表示して横スクロールを発生させない（`AC-CAL-012-5`と両立する）。
- `AC-CAL-004-1` ホームの集計対象は、自分以外のアクティブメンバーが0人なら「グループ」「自分」の2枠、1人なら相手の表示名の直接リンクを加えた3枠、2人以上なら3枠目をメンバー選択欄とする。320px・375px・1280pxで1行を維持し、44px以上のタップ領域を持つ。
- `AC-CAL-004-2` メンバー選択欄の候補は自分以外のアクティブメンバーだけとし、選択中の表示名と`aria-current`を示す。自分のmembershipを`scope=member`で指定した場合は「自分」枠を選択状態とする。対象切替は選択月を維持して`day`を解除し、グループ・自分への切替では`member`を付けない。選択欄は利用者が開いたときだけ展開し、同じ候補の再選択でも閉じる。
- `AC-CAL-004-3` 2枠は等幅とする。3枠では「グループ」「自分」の文字を省略せず、余った幅は3枠へ均等に配分し、幅が足りないときは3枠目だけを縮めて末尾を省略する。320pxでも3枠目は「グループ」枠以上の幅を持ち、省略時もaccessibility nameに表示名の全文を残す。選択欄の枠は開閉を示す印を持ち、印をaccessibility nameへ含めない。
- `AC-CAL-012-1` 日付セルへ、その日の収入合計を「＋」表記と収入用の色で表示する。収入0円の日には表示せず、色だけで種別を伝えない。
- `AC-CAL-012-2` 収入の集計対象は、`scope=group`では全収入、`scope=self|member`では対象メンバーが受取者の収入とする。
- `AC-CAL-012-3` 月間の収入合計を「収入」と明記した支出とは別の値として表示し、支出合計（CAL-009）へ合算しない。
- `AC-CAL-012-4` 日別取引sheetへ収入取引を種別が分かる形で表示する。収入に負担内訳・対象者の支出額は表示しない。
- `AC-CAL-012-5` 通常の文字サイズでは日付セルの収入は`+999,999`まで1行で表示し、320px・375px・1280pxでセル外へのはみ出し・数字の欠落・横スクロールを発生させない。支出も999,999円まで同じ1行規則とし、1,000,000円以上はセル幅に収まらない場合だけ桁区切りの直後で折り返す。今日・選択日も同じ規則とする。
- `AC-CAL-013-1` 集計領域へ、同じ月・集計対象の支出合計、収入合計、収支差額（収入 − 支出）を表示する。
- `AC-CAL-013-2` 差額は金額の前へ符号（黒字は`＋`、赤字は`−`、0円は`±`）を表示し、符号と数字だけで黒字・赤字が判別できる。
- `AC-CAL-013-3` `scope=self|member`では支出側を対象メンバーの負担額、収入側を対象メンバーが受取者の収入として集計し、差額をその差とする。ラベルはすべての集計対象で「支出」「収入」「収支」に統一し、集計領域へ支払額を並記しない（CAL-010）。
- `AC-CAL-013-4` 収入0円、支出0円、両方0円の月でも例外を出さず、意味の分かる表示にする。
- `AC-CAL-013-5` 差額は最小通貨単位の整数で計算し、浮動小数点を使わない。金額文字列はlocale実装に依存せず、server renderとclient hydrationで一致する。
- `AC-CAL-013-6` 日付選択（`day`）の変更では月間の支出・収入・差額を再取得せず、`AC-CAL-001-17`の即時反映を維持する。
- `AC-CAL-014-1` 表示月が当月と異なる場合だけ「今日」操作を表示し、押すとグループのタイムゾーン上の当月へ移動する。`scope`と`member`は維持し、`day`は付けない。
- `AC-CAL-014-2` 表示月が当月の場合は「今日」操作を表示せず、操作の有無で月移動行・集計領域・カレンダー行の高さと日番号の位置が変わらない。
- `AC-CAL-014-3` 「今日」操作は44 x 44 CSS pixel以上のタップ領域と文字labelを持ち、accessibility nameに移動先の月を含む。
- `AC-CAL-014-4` 当月の判定はサーバーがグループのタイムゾーンで決定した値を使い、端末時刻を使わない。
- `AC-CAL-016-1` 日付セルの日番号は、週開始曜日が日曜・月曜のどちらでも実際の曜日に基づき、土曜日を薄い青、日曜日を薄い赤で表示する。平日には曜日による色を付けず、曜日見出しの文字色・セル背景・金額の色は曜日で変えない。
- `AC-CAL-016-2` 曜日の色は補助情報とし、曜日見出しの文字（土・日）で曜日を判別できる。今日の表示（塗りつぶし）は土日でも維持し、選択日・hover時の表現は変えない。選択月外の土日は弱い表示のまま色相だけを区別する。
- `AC-CAL-016-3` 曜日の色付けで日番号・金額の位置と行の高さを変えない。曜日はサーバー側のグリッド生成で決定した値を使い、Client Componentで`Date`から再計算せず端末のtimezoneに依存しない。

- `AC-CAL-005-1` 日別取引の簡易履歴に支出・収入・定期取引のメモ全文を表示する。未記入・空白のみでは欄を表示せず、改行を保持し長い文字列を折り返す。日付切り替え時の追加取得は行わず、320px・375px・1280pxで横にはみ出さない。

- `AC-CAL-005-2` 日別履歴の固定費行では、固定費の名称をカテゴリ・金額とは別の行に全文表示し、同じカテゴリの固定費を識別できる。支出・収入に適用し、メモも引き続き表示する。長い名称は折り返し、日付選択時の追加取得は行わない。
- `AC-CAL-005-3` 日別取引sheetの見出しは、選択日の支出合計が1円以上の日は支出合計を主見出しとし、収入があれば「収入 ＋￥〇〇」を補助行に添える。支出合計が0円で収入がある日は「収入 ＋￥〇〇」を収入用の色で主見出しとし、支出額「￥0」の行を表示しない。支出も収入も無い日は従来どおり「￥0」と空状態の文言を表示する。いずれもClient側で金額を再計算せず、色だけで種別を伝えない。

## UC-008 履歴を閲覧・絞り込みする

基本フロー:

1. アクティブメンバーが選択中グループの履歴画面を開く。
2. サーバーが認証・所属・絞り込み条件を検証し、取引一覧と次ページcursorを返す。
3. 利用者が絞り込みを変更するか、「さらに読み込む」で続きを取得する。

受け入れ条件:

- `AC-HIS-001-1` 選択中グループの取引を、取引日の新しい順、同一取引日内は作成日時の新しい順に表示する。
- `AC-HIS-001-2` 削除済み取引を通常の履歴一覧へ表示しない。
- `AC-HIS-001-3` アクティブメンバーでないユーザーへ履歴データを返さない。
- `AC-HIS-002-1` 月、種別、カテゴリ、受取者、支出した人（負担メンバー）の絞り込みをサーバーで検証し、組み合わせて適用できる。絞り込みsheetの各条件は選択と同時に適用し、「絞り込みを適用」ボタンを置かない。支払者条件はURLパラメータとして引き続き検証するが、絞り込みsheetと適用中条件へ表示しない（`TXN-018`）。
- `AC-HIS-002-2` 不正な絞り込み値は暗黙補正せず、取引データを含まない検証エラー状態を表示する。
- `AC-HIS-003-1` 履歴のshortcut chipは「今月」「支出」「収入」「自分の支出」の4つを横1行に置き、「自分の利用」「自分が支払った」「自分が負担」のchipを表示しない。375pxでは横scrollで全chipへ到達でき、320pxでもchipが欠落しない。
- `AC-HIS-003-2` 「自分の支出」は現在メンバーをmember条件へ設定し、適用中は解除できる。月・カテゴリ等の他条件を維持し、cursorは解除する。
- `AC-HIS-003-3` 支出した人は絞り込みsheetの「支出した人」からも選べ、適用中条件は「支出した人 〇〇」と表示する。member指定時の支出は対象者の負担額を「〇〇の支出」として主表示し、取引金額を「取引全体」として補足する。例えば相手が支払った6,000円を半分負担なら主金額は3,000円。未指定・収入は従来の取引金額を表示し、追加ページ・再読み込みでも同じ規則を使う。
- `AC-HIS-003-4` 絞り込みを使っても、所属していないグループを検索できない。
- `AC-HIS-003-5` 「今月」はグループのタイムゾーン上の今月を`month`条件へ設定し、今月が適用中なら解除する。「支出」「収入」は`type`条件を排他的に切り替え、適用中のchipを再タップすると解除する。いずれも他条件を維持しcursorを解除し、適用中は`aria-current`で示す。
- `AC-HIS-005-1` 一覧は標準30件・最大100件のcursor paginationとし、offset paginationを使用しない。
- `AC-HIS-005-2` 「さらに読み込む」は絞り込み条件を維持したまま次ページを取得し、行を重複・欠落させない。
- `AC-HIS-006-1` 一覧は取引日ごとの日付見出し（当年は「9月8日（火）」、他年は「2025年12月31日（水）」）で束ね、見出しの下の行には日付を繰り返さない。「さらに読み込む」で同じ日付の行が続く場合は見出しを重複させず既存の日付グループへ追記する。
- `AC-HIS-006-2` 各行はカテゴリ色・カテゴリ名・メモ・金額の1〜2行構成とし、内訳は各人の金額を列挙せず、支出した人の表示名で表す（`AC-HIS-007-1`）。収入は「収入」の印と受取者を表示する。支出の支払者と「負担」の語は表示しない（`AC-TXN-018-3`）。
- `AC-HIS-006-3` 行全体を編集画面へのリンクとし、行内に全幅の「編集」ボタンを置かない。リンクのアクセシブル名に日付・カテゴリ・金額（member指定時は「〇〇の支出」の金額と取引全体）と、支出なら支出した人の表示名（「山田・佐藤」）、収入なら「受取者 〇〇」を含め、タップ領域の高さは44 CSS pixel以上、キーボードのfocusとEnterで遷移できる。
- `AC-HIS-006-4` member指定時の支出行は`AC-HIS-003-3`の主金額規則（対象者の負担額を主金額、取引全体を補足）を新しい行構成でも満たし、375 x 812の初期viewportへ8件以上の行が入る。ページ見出しから最初のchipまでの空きはホーム・分析と同じ見出し構成にそろえる。
- `AC-HIS-007-1` 支出行の2行目に支出した人の表示名を表示する。1人なら「山田」、2人以上へ配分された支出は「・」で連結して「山田・佐藤」とし、「支出した人」の見出し語、人数だけの要約（「2人で分割」）、各人の金額は表示しない。削除済みメンバーは履歴用表示名で表す。収入は「受取者 〇〇」を維持し、「負担」「支払者」の語は表示しない（`AC-TXN-018-3`）。
- `AC-HIS-007-2` 行リンクのアクセシブル名に支出した人の表示名を、受取者と同じ位置（金額の後、メモの前）で含める。member指定時の支出行も同じ。行のアクセシブル名に「支出した人」を含めず、絞り込みsheetのラベル「支出した人」と衝突させない。
- `AC-HIS-007-3` メモと支出した人の表示名が同じ行にある場合、表示名は行幅の7割を上限として省略記号で切り、メモは残り幅で省略記号付きに縮める。メモが無い行では表示名が行幅全体を使える。320px・375px・1280pxで横overflowを出さず、追加ページ・再読み込みでも同じ規則で表示し、375 x 812の初期viewportに8件以上の行が入る目標（`AC-HIS-006-4`）を維持する。
- `AC-HIS-008-1` chipとsheetでの条件変更は、ページ全体の再読み込みやServer Componentの再取得を行わず、認可済みの薄いServer Action経由で一覧データだけを取得して置き換える。ページheader、chip、適用中条件、下部ナビゲーションは待機表示へ置き換えず同じ要素を維持し、一覧領域だけを`aria-busy`の待機表示（表示済みの行を薄く残す）にする。
- `AC-HIS-008-2` 変更後の条件をcursorを除いてURLのsearch paramsへ`pushState`で保存する。再読み込み・URL共有では同じ条件をサーバーで検証して復元し、ブラウザの戻る／進むではURLの条件に従って一覧を再取得する。
- `AC-HIS-008-3` 取得失敗時と、サーバーで不正と判定された条件では、表示済みの行と適用中の絞り込み表示を消さず、エラーメッセージと再試行操作を表示する。不正な条件を暗黙補正しない。
- `AC-HIS-008-4` 月の指定は絞り込みsheetの「前の月」「次の月」と「今月」chipで行い、選択中の月をアプリが「2026年9月」の形式で表示する。未指定時は「すべての月」と表示し、前後移動は今月を基準に始める。「解除」で月条件を外す。`<input type="month">`を使わない。
- `AC-HIS-008-5` 絞り込みsheetは900px未満でbottom sheet（modal dialog）として画面下部から開き、focusを閉じ込め、「閉じる」・背景タップ・Escapeで閉じ、閉じた後は開いた操作へfocusを戻す。開閉操作は「絞り込み」とし、適用中の件数を添える。900px以上では絞り込み列の常設パネルとして表示し、開閉操作と「閉じる」を表示しない。

## UC-009 CSVを出力する

- `AC-EXP-001-1` 選択中グループと指定された絞り込み範囲だけを出力する。
- `AC-EXP-001-2` 取引日、種別、金額、カテゴリ、支払者または受取者の表示名、負担内訳、メモを含める。
- `AC-EXP-001-3` ユーザー入力文字列の表計算ソフト数式プレフィックスを無害化する。
- `AC-EXP-001-4` ファイル名に、安全な形式へ変換したグループ名と期間を含める。
- `AC-EXP-001-5` 認可されていない出力要求へデータを返さない。

## UC-010 収入を登録する

例: メンバーBが給与300,000円を受け取る。

- `AC-TXN-013-1` 収入には、選択グループのアクティブな受取者が1人必要である。
- `AC-TXN-013-2` MVPの収入に支出用の負担額を設定できない。
- `AC-TXN-013-3` 収入は履歴へ表示し、標準の支出カレンダー合計から除外する。
- `AC-TXN-013-4` 支出の支払者を収入の受取者として暗黙に解釈せず、種別に応じた項目を明示する。
- `AC-TXN-013-5` 同じ`client_request_id`で再送しても収入を重複作成しない。
- `AC-TXN-013-6` 収入の編集は受取者のアクティブ所属と収入カテゴリを再検証し、取引の種別は変更できない。
- `AC-TXN-013-7` 登録時の種別切替は支出と収入の2つの選択肢だけを等幅で表示し、空の選択枠を残さない。

## UC-011 グループ所有権を維持する

- `AC-GRP-010-1` グループ作成時に、アクティブownerを1人だけ作成する。
- `AC-GRP-010-2` ownerはアクティブなadminまたはmemberをownerへ昇格できる。
- `AC-GRP-010-3` 最後のアクティブownerは、降格・削除・脱退できない。
- `AC-GRP-010-4` 別ownerの昇格後、以前のownerは降格または脱退できる。
- `AC-GRP-010-5` 所有権の同時更新でも、ownerが0人の状態をcommitできない。

## UC-012 登録・ログイン・セッションを管理する

基本フロー:

1. ユーザーが通常のtop-level navigationでGoogleログイン開始Route Handlerへ遷移する。
2. Googleがアカウント選択画面を表示し、ユーザーが選んだアカウントで本人確認したうえで、Supabase AuthがPKCE callbackを処理する。
3. 登録前フックがGoogle providerと許可リストを照合し、対象外はAuthユーザー作成前に拒否する。
4. 許可された初回ログインでSupabase Authのユーザーと`profiles`行を作成する。
5. アプリがcookieのsession、Google provider、許可リストを再検証し、保護画面を表示する。
6. ユーザーがログアウトするとsessionを破棄し、ログイン画面へ戻る。

受け入れ条件:

- `AC-AUTH-001-1` 未認証画面はGoogleログインだけを表示し、メールアドレス、ID、パスワード、OTPの入力欄を表示しない。
- `AC-AUTH-001-2` GoogleログインはPKCE callbackでcodeをsessionへ交換し、callback後のredirectは構成済みの公開サイトoriginと許可済みの相対pathからだけ生成する。Auth cookieを設定する応答は共有cacheへ保存させない。
- `AC-AUTH-001-3` 許可された初回ログイン時、認証ユーザーと同じIDの`profiles`行を1件だけ作る。
- `AC-AUTH-001-4` OAuth失敗、許可対象外、Auth停止を区別した安全なメッセージを表示し、tokenやproviderの内部詳細を表示しない。
- `AC-AUTH-001-5` 初回プロフィールの表示名は、Googleが返す検証済みmetadataの`full_name`、`name`、「ユーザー」の順で最初の空でない値を使い、前後空白を除去して50文字以内にする。
- `AC-AUTH-001-6` OAuth認可URLは、生成元が構成済みの内部または公開Supabase originで、pathが`/auth/v1/authorize`の場合だけ、構成済みの公開Supabase originへ変換してブラウザへ返す。Docker内部hostname、想定外origin・path、認証情報またはfragmentを含むURLを拒否する。
- `AC-AUTH-001-7` OAuth開始Route Handlerは検証済みの戻り先だけを受け付け、PKCE verifier cookieを応答へ設定した後に公開Supabase認可URLへredirectする。Googleからアプリへ戻ったcallbackは同じcookieを使ってcodeをsessionへ交換でき、cookie欠損時はsessionを作らず安全なOAuth失敗として扱う。
- `AC-AUTH-001-8` OAuth開始要求のoriginが構成済みの公開サイトoriginと異なる場合、PKCE cookieを発行せず、検証済みの戻り先だけを含む公開サイトorigin上のOAuth開始Routeへredirectする。canonical originへ揃えた後の応答でPKCE cookieを発行し、callbackと同じhostへ保存する。
- `AC-AUTH-001-9` ブラウザに失効済みまたは無効なAuth cookieが残った状態でOAuthログインを完了した直後でも、保護画面の初回表示をserver errorにしない。Proxyは認証境界（`/auth`配下のRoute Handler）に対してsession refreshを行わず、Server Componentの読み取りqueryは認証起因の失敗（期限切れ・無効JWT）をserver errorとして扱わず、未認証としてログイン誘導へ合流させる。
- `AC-AUTH-001-10` access tokenの期限が切れた後も、refresh tokenが有効な間は再ログインを求めない。session更新を行った境界は、rotate後のtokenを同じ応答のcookieへ書き戻し、ブラウザが保持するtokenと不一致にしない。
- `AC-AUTH-001-11` 認証済みで`start_url`（`/`）またはOAuth開始Routeへ到達した場合、ログイン画面とGoogle認証を経由せず、ホームまたは検証済みの戻り先を表示する。
- `AC-AUTH-001-12` 失効したAuth cookieが残った状態でも、1回のGoogleログインでsessionが成立する。callbackは既存のsession cookieを参照せずPKCEのcode verifierだけでcodeを交換し、交換したsession cookieを同じ応答内の削除cookieで打ち消さない。
- `AC-AUTH-001-13` 認証済みでホーム（`/app`）を開く要求では、プロフィールとグループ一覧の取得完了を待たずにroute shellとskeletonを先に返し、ホーム画面インストール版の起動直後を含めて全面が白いままの状態を作らない。skeletonは`aria-busy`で読み込み中を伝え、確定後の画面と同じ配置（header、プロフィール、グループ一覧）と375px・1280pxのカラム構成を保つ。
- `AC-AUTH-006-1` OAuth開始Route Handlerは、Googleへ転送する認可要求に`prompt=select_account`を付与し、ブラウザにGoogleアカウントのsessionが1件だけ残っている場合でも、Googleのアカウント選択画面を毎回表示する。`login_hint`など特定アカウントを既定にするパラメータは付けない。
- `AC-AUTH-006-2` アプリの有効なsessionがある間は`AC-AUTH-001-11`に従いGoogle認証を経由しないため、別のGoogleアカウントへ切り替える手順は「ログアウト」→「Googleでログイン」→Googleのアカウント選択とし、ログイン画面と利用者向けガイドにこの手順を示す。切り替え後も許可リストの照合（`AC-AUTH-001-4`）は変わらない。
- `AC-AUTH-002-1` ログインユーザーは自分のプロフィールを取得・更新でき、別ユーザーとして更新できない。
- `AC-AUTH-003-1` ログアウト後は保護画面を閲覧できず、ログイン画面へ遷移する。
- `AC-AUTH-004-1` 未認証で保護画面へアクセスすると、ログイン後の戻り先を安全な相対pathとして保持してログイン画面へ遷移する。
- `AC-AUTH-004-2` 外部URL、protocol-relative URL、制御文字を含む戻り先を拒否し、標準の保護画面を使う。
- `AC-AUTH-004-3` サーバーの重要な読み取り・更新は、cookie上の未検証session情報ではなく検証済みclaimsまたはAuthユーザーを使用する。
- `AC-AUTH-004-4` 保護画面の認証確認でSupabase Auth・PostgRESTが応答不能（接続失敗、timeout、5xx）な場合、未認証として扱わず、ログイン画面へ遷移しない。Proxyはredirectせずに要求を通し、Server Componentの読み取りは認証起因の失敗（`AC-AUTH-001-9`）と区別してserver errorにし、routeのerror境界が「読み込めませんでした」と「再試行」を表示する。sessionが存在しない、無効、または期限切れで更新できない場合だけ、従来どおりログイン画面へ遷移する。
- `AC-AUTH-004-5` Proxyの認証確認は総待機時間5秒で打ち切り、超過を応答不能として扱う。Server Componentの取得中はroute-level loadingを表示し、取得失敗後にerror境界へ切り替える。error境界の「再試行」は同じURLを再取得し、認証状態や戻り先を書き換えない。
- `AC-AUTH-005-1` ユーザー向けコードからメール・パスワード登録、ログイン、再設定の画面と処理を削除する。旧URLはログイン画面へ安全に転送する。
- `AC-AUTH-005-2` Authのメール、電話番号、匿名ログインを無効化し、ローカル・本番のアプリ構成からSMTPとMailpitを除外する。
- `AC-AUTH-005-3` 許可リストは大文字小文字と前後空白を正規化して重複のない有効なメールアドレス1件以上を受け付け、件数の上限を設けない。不正値、重複、空値を含む場合は全件を無効にする。Git、クライアントbundle、log、画面に値を公開しない。

## UC-013 メンバーの権限を管理する

操作者: owner。

受け入れ条件:

- `AC-GRP-007-1` ownerだけがメンバーの権限を変更でき、adminとmemberには変更操作を提供しない。
- `AC-GRP-007-2` 権限は所有権制約の範囲でowner、admin、memberへ変更でき、最後のアクティブownerを降格できない。
- `AC-GRP-007-3` 操作者は検証済みの認証セッションから決定し、別グループや削除済みのmembershipを対象にできない。
- `AC-GRP-008-1` ownerだけがowner以外のアクティブメンバーを削除できる。
- `AC-GRP-008-2` 削除は物理削除せず所属を非アクティブ化し、過去取引の支払者・受取者・負担者の表示名参照を維持する。
- `AC-GRP-008-3` 削除済みメンバーを新規取引の支払者・受取者・負担者の入力候補へ表示しない。
- `AC-GRP-008-4` 権限変更・削除の再送は冪等に処理し、同時更新でもownerが0人の状態をcommitしない。

## UC-014 カテゴリを管理する

操作者: ownerまたはadmin。

受け入れ条件:

- `AC-CAT-002-1` ownerまたはadminだけがカテゴリを追加、名称変更、並び替え、アーカイブでき、memberには操作を提供しない。
- `AC-CAT-002-2` カテゴリ名は前後空白を除いた1〜30文字とし、同一グループ・同一種別内で名称が重複する追加・変更を拒否する。
- `AC-CAT-002-3` 並び替えは同一グループ・同一種別のアクティブカテゴリ全体の順序として検証し、順序の欠落・重複・別グループのカテゴリ混入を拒否する。
- `AC-CAT-002-4` 別グループや存在しないカテゴリを対象とする操作を拒否する。
- `AC-CAT-002-5` カテゴリはドラッグハンドル（44 x 44 CSS pixel以上、pointer操作）で任意の位置へ並び替えでき、ドラッグ中は挿入位置を視覚表示し、保存失敗時は元の順序へ戻してエラーを表示する。ハンドルはfocus可能とし、上下矢印キーで1つずつ移動できる。クライアントは対象カテゴリと移動先位置だけを送り、全体順序はサーバーが組み立てて検証する。
- `AC-CAT-002-6` owner/adminは編集パネルからカテゴリの名称と色を1つの保存操作で変更できる。色は定義済みパレットの値だけを受け付け、サーバーで再検証する。
- `AC-CAT-002-7` カテゴリ色のパレットは18色のtoken（既定の`food`〜`extra`の9色と、追加の`orange`、`olive`、`mint`、`sky`、`indigo`、`navy`、`rose`、`wine`、`charcoal`）で構成し、各tokenへ色名のaccessibility labelを付ける。tokenと色値の対応は共通design tokenとして1箇所で定義し、カテゴリ管理、取引入力、履歴、カレンダー、予算、分析の各画面が同じ色で描画する。DB制約・更新関数・入力schemaは同じ18色だけを受け付け、パレット外を拒否する。
- `AC-CAT-003-1` 削除（論理アーカイブ）したカテゴリを新規取引の選択肢へ表示しない。
- `AC-CAT-003-2` 削除（論理アーカイブ）しても過去取引のカテゴリ表示を変更しない。
- `AC-CAT-003-3` 削除（論理アーカイブ）の再送は冪等に処理する。

## UC-015 グループ内を移動する

基本フロー:

1. アクティブメンバーが選択中グループの画面を開く。
2. モバイルでは下部、広い画面では適応型の共通ナビゲーションを表示する。
3. 利用者がホーム、履歴、入力、分析、設定のいずれかへ移動する。

受け入れ条件:

- `AC-NAV-001-1` モバイルの共通下部ナビゲーションは「ホーム」「履歴」「入力」「分析」「設定」の5項目だけをこの順序で表示し、「メンバー」を独立項目として表示しない。
- `AC-NAV-001-2` 各項目は44 x 44 CSS pixel以上のタップ領域、文字label、現在地を示す`aria-current`と色以外の視覚表現を持ち、下端のsafe areaと重ならない。
- `AC-NAV-001-3` 1280px幅では同じ5項目の意味と順序を保ったサイドナビゲーションへ適応し、本文を過度に狭めない。
- `AC-NAV-001-4` 履歴、メンバー、カテゴリなど共通ナビゲーション配下の画面は、headerへホーム・グループ一覧など同じ画面間導線を重複表示しない。絞り込み、権限変更など各画面固有の操作は維持する。
- `AC-NAV-002-1` ホームは選択中グループの当月・グループ全体の月間カレンダーへ遷移する。
- `AC-NAV-002-2` 通常の文字サイズにおける375 x 812 CSS pixelでは、選択中グループ名、月移動、集計対象、月間合計、曜日、42日分のカレンダー、下部ナビゲーションを初期viewport内へ表示し、ページ全体の縦スクロールを発生させない。
- `AC-NAV-002-3` 320px幅、375 x 667、390px幅、430px幅および将来の異なるviewportでは、特定機種判定を使わず可変grid、`dvh`、safe areaで適応し、収まらない高さでは欠落・重なりの代わりに縦スクロールを許可する。
- `AC-NAV-003-1` 「入力」は他項目と同じデザインの項目として表示し、選択中グループの取引入力routeへ遷移する。
- `AC-NAV-004-1` 設定ハブはプロフィール・グループ切替と作成・メンバー・カテゴリ・固定費・CSV出力・グループ設定・ログアウトの入口を表示し、各処理は遷移先でも認証と権限を再確認する。
- `AC-NAV-004-2` メンバー画面、カテゴリ画面、固定費画面を直接開いた場合も共通ナビゲーションの現在地は「設定」として示す。概要分析画面を開いた場合は「分析」を現在地として示す。

## UC-016 固定費を管理する

操作者: 作成・編集・終了はownerまたはadmin。閲覧はアクティブメンバー。

基本フロー:

1. owner/adminが設定ハブから固定費画面を開く。
2. 種別、名称、金額、毎月の日付、開始月、任意の終了月、カテゴリ、収入の受取者、支出の分け方を入力する。支出の支払者は表示せず自動設定する（`TXN-018`）。
3. 保存前に「毎月○日に○円」と内訳を確認して保存する。
4. ホームカレンダーの対象月に、展開された固定費が合計と日別表示へ含まれる。
5. 金額や日付が変わった場合は編集し、続かなくなった場合は終了する。

受け入れ条件:

- `AC-REC-001-1` ownerまたはadminだけが固定費を作成、編集、終了でき、memberには操作を提供せず、サーバーとDB関数でも権限を再確認する。
- `AC-REC-001-2` 名称は前後空白を除いた1〜40文字、金額は1円以上の整数、毎月の日付は1〜28の整数だけを受け付け、範囲外の値をサーバーで拒否する。
- `AC-REC-001-3` 開始月と終了月は`YYYY-MM`として検証し、終了月が開始月より前の入力を拒否する。終了月は未指定を許可する。
- `AC-REC-001-4` 支出はアクティブな支払者と負担額合計が金額と一致する負担を必須とし、収入はアクティブな受取者を必須として負担行を作らない。カテゴリは同一グループの同一種別かつ未アーカイブに限る。
- `AC-REC-001-5` 別グループ・存在しないカテゴリ・メンバー・固定費を対象とする操作を拒否し、代替値へ暗黙に置換しない。
- `AC-REC-002-1` 対象月が開始月以降かつ終了月以内の固定費だけを、その月の`day_of_month`日へ1件だけ展開する。対象期間外の月へ表示・集計しない。
- `AC-REC-002-2` 展開はホームカレンダーの月間合計・日付セル・日別取引sheetへ、単発取引と重複せずに反映される。グループ対象では取引金額、メンバー対象では負担額を合計する。
- `AC-REC-002-3` 日別取引sheetでは展開取引に「固定費」と分かるlabelを表示し、単発取引と識別できる。展開取引へ編集・削除の操作を提供しない。
- `AC-REC-002-4` 展開結果をDBへ保存せず、job、scheduler、queue、retry、occurrenceテーブル、内部job endpointを追加しない。
- `AC-REC-003-1` 編集はversionを使い、古い状態からの更新を競合として返す。編集・終了は確定済みの単発取引を変更しない。
- `AC-REC-003-2` 終了した固定費は翌月以降展開せず、一覧では終了済みと分かる表示で残る。
- `AC-REC-004-1` 固定費が1件以上登録されたグループでも固定費画面を開け、一覧に登録済みの固定費が表示される。一覧queryが取得する列は、行検証schemaが要求する列をすべて含む。
- `AC-REC-005-1` 固定費の作成・編集フォームの金額欄は`inputmode="none"`としてOSの仮想キーボードを開かず、画面内テンキーの1〜9、0、00で入力し、1文字削除できる。先頭に0を作らず、安全な整数の上限を超える桁を追加しない（取引入力の`AC-TXN-014-4`と同じ規則）。
- `AC-REC-005-2` テンキーは金額欄へfocusまたはタップしたときに開き、金額欄以外の入力欄へfocusしたときに閉じる。テンキー自体の操作では開閉状態と入力済みの金額を変えない。閉じている間は、金額欄から再び開けることを画面上の説明で示す。
- `AC-REC-005-3` 物理キーボードとスクリーンリーダーからの金額入力・確認は開閉状態にかかわらず維持し、テンキーの各キーはフォームを送信しない。編集フォームでは保存済みの金額を初期値として表示する。
- `AC-REC-005-4` テンキーは金額欄の直下へフォーム内に静的に配置し、画面下部ナビゲーションを覆わない。開いたときにキーが下部ナビゲーションへ隠れる場合は、隠れない位置まで表示位置を移動する。各キーは375 x 812と320pxで44 x 44 CSS pixel以上のタップ領域を持つ。
- `AC-REC-005-5` 固定費の金額欄は入力中も取引入力（`AC-TXN-014-10`）と同じ3桁区切りで表示する（`128000`と打つと`128,000`。編集フォームの初期値も同じ）。表示用の`input`は`name`を持たず、送信する`amountMinor`は区切りなしの整数のままhidden inputで送る。物理キーボードや貼り付けで入った`,`は無視し、それ以外の文字はこれまでどおりサーバー検証へ委ねる。区切りはlocale実装に依存しない決定的な整形で行い、スクリーンリーダーには区切り込みの値をそのまま読ませる。

## UC-017 概要分析で当月の家計状態を把握する

操作者: アクティブメンバー。

基本フロー:

1. アクティブメンバーが共通ナビゲーションの「分析」から概要分析を開く。
2. 当月・グループ全体の支出、収入、収支と、前月との差額を確認する。
3. 支出カテゴリ上位5件と残りの合計を、円グラフ（既定）または棒グラフと一覧で確認する。
4. 必要に応じて前月・翌月へ移動し、集計対象をグループ・自分・メンバーへ切り替える。自分以外のアクティブメンバーが2人以上いる場合は「メンバー」の選択欄から対象者を選ぶ。
5. 気になる月はホームカレンダーまたは履歴で個別取引を確認する。

受け入れ条件:

- `AC-ANA-001-1` アクティブメンバーだけが対象グループの概要分析を閲覧でき、未認証は`next`付きログインへ、非メンバーは存在を明かさない応答へ落ちる。
- `AC-ANA-001-2` 別グループのID、削除済みmembership、他グループのmembershipを指定しても分析値と存在を取得できない。
- `AC-ANA-002-1` 同じ月・同じ集計対象における概要分析の支出合計・収入合計は、ホームカレンダーの月間合計と一致する。メンバー対象の支出は負担額とし、立て替えた支払額を数えない。
- `AC-ANA-002-2` 収支は同じ月・対象の`収入 − 支出`をJPY整数で計算し、符号を明示して黒字・赤字を色だけで伝えない。
- `AC-ANA-003-1` 前月比は支出・収入・収支のそれぞれについて、前月との差額を符号付きJPY整数で表示する。比較元が0円や負の場合も差額だけを表示し、`比較なし`などの比率の代替表示を行わない。
- `AC-ANA-003-2` 前月の比較元が正の場合でも、前月に対する比率（パーセント）を算出・表示しない。
- `AC-ANA-004-1` 概要分析の支出カテゴリ一覧は、同じ月・対象で支出のあるカテゴリを金額の降順で全件表示し、「その他のカテゴリ」へ丸めない。一覧の金額の合計は同じ月・対象の期間支出と一致する。
- `AC-ANA-004-2` カテゴリ別支出は取引のカテゴリIDで集計し、アーカイブ済みカテゴリも現在の表示名で内訳へ含める。
- `AC-ANA-005-1` 前月・翌月操作で選択月を変更でき、月と集計対象をURLへ保持して再読み込み・URL共有でも同じ表示になる。
- `AC-ANA-005-2` 不正な月、集計対象、membershipの指定は取引を読み込まずにfail closedで拒否し、当月・グループ表示へ戻る導線を表示する。
- `AC-ANA-005-3` 集計対象の枠は、認可済みcontextから得た自分以外のアクティブメンバー数で切り替える。0人は「グループ」「自分」の2枠、1人は3枠目にそのメンバー名の直接リンク、2人以上は3枠目にメンバー選択欄を表示し、枠を折り返さない。3枠の幅配分・省略・開閉の印は`AC-CAL-004-3`と同じ規則に従う。
- `AC-ANA-005-4` メンバー選択欄は自分以外のアクティブメンバーだけを候補とし、利用者が開いたときだけ展開して選択後は閉じる。候補リンクは`month`・`scope=member`・`member={membershipId}`をURLへ保持し、選択中の枠と候補を`aria-current`で示す。
- `AC-ANA-009-1` グラフを表示しない場合も、カテゴリ名、正確な金額、構成比のテキストから同じ値と意味を確認できる。
- `AC-ANA-009-2` 320px幅と375 x 812では横スクロールが発生せず、支出・収入・収支と月移動を片手で操作できる。横スクロール式のカード列を使わない。
- `AC-ANA-009-3` 1280 x 800では同じ情報構造と同じ値を保ったまま利用可能幅へ適応し、コンテンツが過度に横へ伸びない。
- `AC-ANA-010-1` 収入を支出合計、メンバー負担額（画面では支出額）のいずれへも混入させず、別ラベルで表示する。
- `AC-ANA-011-1` 予算値がない月は予算進捗領域を表示せず、予算値がある場合だけ消化額・残額・消化率を同じDTO境界から表示する。
- `AC-ANA-012-1` 概要分析と期間サマリーは同じ認可済み集計サービスを共有し、同じグループ・期間・対象に対して同じ金額を返す。
- `AC-ANA-012-2` 24か月を超える期間、不正な月、開始月が終了月より後の期間サマリー要求をfail closedで拒否し、取引を読み込まない。
- `AC-ANA-014-1` 概要分析の支出カテゴリと詳細分析の支出カテゴリ構成は、初期表示で円グラフを表示し、同じ領域の「円グラフ」「棒グラフ」の切替で横棒グラフへ即時に切り替えられる。切替はページ遷移・取引の再取得・URL変更を伴わず、他の領域を再描画しない。
- `AC-ANA-014-2` 円グラフの扇形は各カテゴリの金額の合計に対する割合から求め、一覧と同じ降順で12時方向から時計回りに並べる。全扇形の合計は一周と一致し、1件だけなら全周、金額0円のカテゴリは扇形を持たない。扇形はカテゴリ色のdesign tokenで塗る。
- `AC-ANA-014-3` 円グラフ・棒グラフのどちらでも、同じ順序・件数の一覧にカテゴリ名、色の印、正確な金額、構成比をテキストで表示し、グラフは`aria-hidden`の装飾とする。切替ボタンは44 x 44 CSS pixel以上で選択中を`aria-pressed`で示し、320px・375 x 812で横スクロールなく収まり、1280 x 800でも同じ値と順序を保つ。
- `AC-ANA-014-4` 一覧は1カテゴリ1行で、色の印、カテゴリ名、正確な金額、構成比を左から同じ行へ並べる。構成比を別の行へ分けず、金額と構成比は折り返し・省略しない。名称が長い場合は名称側を省略記号で切り詰め、320px・375pxでも各行の名称・金額・構成比が欠落しない。棒グラフでは横棒を行の下へ添える。

## UC-018 所属グループが1件のときホームから直行する

操作者: アクティブな所属グループを1件だけ持つログインユーザー。

基本フロー:

1. 利用者がログインを完了する、`start_url`を開く、またはホーム（`/app`）へ遷移する。
2. サーバーが認証済みsessionからアクティブな所属グループを取得する。
3. 所属が1件だけであれば、グループ一覧を表示せずにそのグループの当月・グループ全体のカレンダーへ遷移する。
4. 所属が0件または2件以上であれば、従来どおり作成画面または一覧を表示する。
5. グループ一覧・プロフィール・グループ作成が必要な場合、利用者は設定の「グループを切り替える・作る」など明示的な導線から一覧表示を開く。

受け入れ条件:

- `AC-GRP-011-1` アクティブな所属が1件だけのユーザーが`/app`を開くと、一覧を表示せずに`/groups/{groupId}`（当月・グループ全体）へ遷移する。ログイン完了後の既定の戻り先、認証済みでの`/`・ログイン画面からのホーム転送も同じ結果になる。
- `AC-GRP-011-2` 所属が0件のユーザーには従来どおりグループ作成画面、2件以上のユーザーには従来どおりグループ一覧を表示し、直行しない。
- `AC-GRP-011-3` グループ一覧を明示的に開く導線（設定の「グループを切り替える・作る」、履歴の「グループ一覧」）は`/app?view=groups`へ遷移し、所属が1件でも一覧・プロフィール・グループ作成を表示する。
- `AC-GRP-011-4` 遷移先の判定はサーバー側で認証済みsessionから取得した所属だけを使い、クライアントから渡されたgroupIDや件数を信用しない。`view=groups`以外のquery値は一覧表示への切替として扱わず、既定の判定に従う。
- `AC-GRP-011-5` 直行先のカレンダー画面は従来どおり認証・所属を再確認し、遷移の間に所属を失った場合も他グループの存在や内容を明かさない。

## UC-019 詳細分析で期間の変化と内訳を確認する

操作者: アクティブメンバー。

基本フロー:

1. 概要分析から詳細分析を開く。
2. 3・6・12か月のpreset、または開始月と終了月を選ぶ。
3. グループ全体、自分、指定メンバーから対象を選ぶ。
4. 期間合計、月平均、最大支出月、月別推移（支出・収入を切り替える縦棒グラフ）、カテゴリ構成（円グラフまたは棒グラフ）を確認する。
5. 貯金額の推移（累積収支）の棒グラフで、期間内にいくら貯まったか・減ったかを確認する。
6. グループ対象では、各メンバーの支出額（負担額）・受取額を比較する。
7. グラフと同じ値を月別の数値表で確認する。

受け入れ条件:

- `AC-ANA-006-1` 未指定時は当月を終了月とする直近6か月を表示し、3・6・12か月presetと1〜24か月の任意範囲を選択できる。
- `AC-ANA-006-2` 期間と集計対象をURLへ保持し、再読み込み・URL共有でも同じ条件と値を表示する。
- `AC-ANA-006-3` 不正な月、逆順、24か月超は取引を読み込まずにfail closedで拒否し、既定の6か月表示へ戻る導線を示す。
- `AC-ANA-007-1` グループ全体、自分、同一グループのアクティブメンバーを対象にでき、メンバー対象の支出は負担額として集計する。
- `AC-ANA-007-2` 別グループまたは削除済みmembership、scopeと一致しないmember指定を拒否し、分析値と存在を取得できない。
- `AC-ANA-008-1` 期間の支出・収入・収支合計は月別値の合計と一致し、平均支出は取引0円の月を含む期間月数で割ってJPY整数へ四捨五入する。
- `AC-ANA-008-2` 最大支出月は期間内の月別支出から求め、同額は新しい月、全月0円は「該当なし」とする。
- `AC-ANA-008-3` カテゴリ別支出は期間全体をカテゴリIDで集計し、全カテゴリの合計が期間支出と一致する。アーカイブ済みカテゴリも含める。
- `AC-ANA-008-4` グループ対象では各アクティブメンバーの支出額（負担額）・受取額を別系列で表示し、各列を混在させない。支払額の列は表示しない（`TXN-018`）。self・member対象では重複する比較領域を表示しない。
- `AC-ANA-009-4` 月別推移の支出・収入・符号付き収支を正確なJPY数値表でも表示し、装飾グラフ、hover、色がなくても値と意味を理解できる。
- `AC-ANA-009-5` 320px・375 x 812では横スクロールなしで主要操作を44 x 44 CSS pixel以上にし、数値表を月別行へ適応する。1280 x 800では同じ順序・値を保って複数カラムへ適応する。
- `AC-ANA-009-6` 520px以下の詳細分析では、月別表とメンバー別表の各金額に項目名を見える文字で併記する。広い画面では列見出しで区別し、全幅で表の見出しと値の関連を保持する。補助ラベルによる読み上げの重複と横スクロールを発生させない。
- `AC-ANA-009-7` 開始月・終了月の入力欄は320px・375px・iOS Safariで各列幅に収まり、高さ44px以上、値の左揃えで、隣の入力欄やカードの外へはみ出さない。値が空でも高さが縮まない。
- `AC-ANA-012-3` 詳細分析は概要・期間サマリーと同じ認可済み月次集計を使い、1要求で月次取引を1回だけ読み込む。同じ期間・対象の金額は一致する。
- `AC-ANA-013-1` 各月の累積収支は、開始月の直前を0円として開始月から当該月までの月別収支を順に加算した値と一致し、取引がない月は前月の累積値を引き継ぐ。最終月の累積収支は期間の収支合計と一致する。
- `AC-ANA-013-2` 累積収支は月別数値表の「累積収支」列に符号付きJPY整数で表示し、棒グラフは`aria-hidden`の装飾とする。各月の棒は0円の基準線から正なら上、負なら下へ伸び、月数と同じ本数を等間隔に並べる。グラフは0円の基準線、最大値・最小値、開始月・終了月を文字で添える。赤字の棒は赤系の色で示すが、棒の向きと数値表の符号でも伝え、色だけで黒字・赤字を伝えない。
- `AC-ANA-013-3` 累積収支は期間開始前の残高を含めず、「期間開始時を0円として計算」と画面に注記する。320px・375 x 812で横スクロールを発生させず、1280 x 800でも同じ値と順序を保つ。
- `AC-ANA-015-1` 詳細分析の月別推移は、初期表示で支出の縦棒グラフを表示し、同じ領域の「支出」「収入」の切替で収入の縦棒グラフへ即時に切り替えられる。切替はページ遷移・取引の再取得・URL変更を伴わず、他の領域を再描画しない。
- `AC-ANA-015-2` グラフは横軸を期間内の月、縦軸を金額とし、選択中の系列の各月の金額を0円の基準線から上へ伸びる棒で示す。月数と同じ本数を等間隔に並べ、棒の高さは当該系列の期間内最大値に対する比率とし、最大値と0円のラベルを文字で添える。全月0円でも最小高さの棒を描く。6か月以下では各棒の下に月のラベルを、7〜12か月では終了月を含む1か月おきの月のラベルを、13か月以上では開始月・終了月のラベルを表示する。
- `AC-ANA-015-3` グラフは`aria-hidden`の装飾とし、同じ値を「月別の正確な数値」表の支出・収入列で表示する。切替ボタンは44 x 44 CSS pixel以上で選択中を`aria-pressed`で示し、320px・375 x 812で横スクロールなく収まり、1280 x 800でも同じ値と順序を保つ。

## UC-020 起動時に開くグループを設定する

操作者: 2件以上のグループへアクティブに所属するログインユーザー（1件でも設定できる）。

基本フロー:

1. 利用者が対象グループの設定画面（`/groups/{groupId}/settings`）を開く。
2. 「起動時に開くグループ」の項目で「このグループを起動時に開く」を実行する。
3. Server ActionがgroupIDをUUID検証し、DB関数が検証済みGoogle sessionの本人であることと対象グループへのアクティブ所属を再確認して、ユーザーごとに1件の設定を保存する。
4. 以後、利用者がログインを完了する、`start_url`を開く、またはホーム（`/app`）へ遷移すると、グループ一覧を表示せずにそのグループの当月・グループ全体のカレンダーへ遷移する。
5. 別のグループの設定画面で同じ操作を行うと、起動時に開くグループがそのグループへ置き換わる。既定グループの設定画面で「解除する」を実行すると未設定へ戻り、`UC-018`の判定に従う。

受け入れ条件:

- `AC-GRP-012-1` 設定画面のアカウント項目に「起動時に開くグループ」を表示する。対象グループが起動時に開くグループでなければ「このグループを起動時に開く」操作を、起動時に開くグループであれば現在の設定であることと「解除する」操作を表示する。操作は他画面へ遷移せず、成功・失敗を同じ画面へ表示し、表示中の状態を結果に合わせて更新する。
- `AC-GRP-012-2` 起動時に開くグループはユーザーごとに1件（`user_preferences.default_group_id`）として保存し、更新は`security definer`のDB関数だけで行う。DB関数は検証済みGoogle sessionの本人であることと許可リストを再確認し、設定時は対象グループへのアクティブ所属を再確認する。非メンバー、削除済み所属、存在しないgroupIDは同じ権限エラーで拒否し、グループの存在を明かさない。解除は`null`へ更新する。Server Actionはbind引数のgroupIDと操作種別を未信頼入力としてschema検証する。
- `AC-GRP-012-3` 起動時に開くグループが設定され、その所属がアクティブであれば、`/app`（ログイン完了後の既定の戻り先、認証済みでの`/`・ログイン画面からのホーム転送を含む）は所属件数にかかわらず`/groups/{defaultGroupId}`（当月・グループ全体）へ遷移する。`view=groups`では`AC-GRP-011-3`のとおり一覧を表示する。
- `AC-GRP-012-4` 起動時に開くグループが未設定、または設定先の所属を失っている（削除済み・存在しない）場合は、`AC-GRP-011-1`〜`AC-GRP-011-2`の判定（所属1件なら直行、0件は作成画面、2件以上は一覧）に従う。無効になった設定は自動で書き換えず、他グループの存在や内容を明かさない。遷移先の判定はサーバーが認証済みsessionから取得した所属と本人の設定だけを使い、クライアントから渡された値を信用しない。
- `AC-GRP-012-5` グループ一覧（`/app?view=groups`）では起動時に開くグループの行にだけ「起動時に開く」の表示を付け、一覧のリンク先と並び順は変えない。
- `AC-GRP-012-6` `user_preferences`は本人だけがselectでき、同じグループの他メンバーや別ユーザーは行を参照・作成・更新・削除できない。テーブルへの直接insert・update・deleteは`authenticated`へ許可しない。

## UC-020 月間予算とカテゴリ予算を管理する

操作者: 設定・改定・停止はownerまたはadmin。閲覧はアクティブメンバー。

基本フロー:

1. owner/adminが設定ハブの「予算」から予算画面を開く。
2. 当月以降の月を選び、グループ予算と任意のカテゴリ別予算を入力する。送信前にカテゴリ予算の合計と未配分額を確認する。
3. 保存すると、その月を開始月とする改定が保存され、後続月へ継続する。
4. アクティブメンバーは予算画面と分析概要で、予算額、実績、残額、消化率、状態（順調・注意・超過）を確認する。
5. 金額を変えたい月から改定し、予算をやめる月から停止する。過去月の予算は変更しない。

受け入れ条件:

- `AC-BUD-001-1` ownerまたはadminだけが予算を設定・改定・停止でき、memberには操作を提供せず、Server ActionとDB関数でも権限を再確認する。
- `AC-BUD-001-2` memberと非メンバーはDB関数でも直接のtable更新でも予算を更新できず、非メンバーは別グループの改定・内訳をselectできない。
- `AC-BUD-001-3` 開始月はグループのタイムゾーン上の当月以降だけを受け付け、過去月を開始月とする改定の作成・変更・停止をDB関数で拒否する。
- `AC-BUD-002-1` 収入カテゴリ、別グループのカテゴリ、アーカイブ済みカテゴリ、同一改定内で重複するカテゴリを新しい内訳へ設定できない。
- `AC-BUD-002-2` カテゴリ予算の合計がグループ予算を超える要求を拒否し、改定本体と内訳のどちらも部分的に更新しない。
- `AC-BUD-003-1` アクティブメンバーは選択月に適用される予算と実績を閲覧でき、未認証は`next`付きログインへ、非メンバーは存在を明かさない応答へ落ちる。
- `AC-BUD-003-2` 予算未設定の月は説明を表示し、不正な`month`は予算と取引を読み込まずに検証エラーと当月へ戻る導線だけを表示する。
- `AC-BUD-004-1` 予算実績は同じ月のグループ支出合計（固定費の展開を含む）と一致し、収入とメンバー負担額を含めない。
- `AC-BUD-004-2` カテゴリ実績は取引のカテゴリIDで集計し、カテゴリ予算を持たないカテゴリの支出はグループ実績にだけ含める。
- `AC-BUD-005-1` 新しい改定は開始月より前の月の予算表示を変更せず、開始月以降は次の改定の前月まで同じ予算を適用する。
- `AC-BUD-006-1` 停止改定の開始月以降は予算未設定として表示し、過去の予算履歴を削除しない。停止済みの月へ再設定すると同じ開始月の改定が有効に戻る。
- `AC-BUD-007-1` 80%と100%の境界で「順調」「注意」「超過」を正しく表示し、色がなくても状態を判別できる。判定は整数比較で行い、表示用の四捨五入値を使わない。
- `AC-BUD-007-2` 残額は`予算額 − 実績`とし、超過時は超過額として明示する。消化率は整数パーセントへ四捨五入し、100%超も切り詰めない。
- `AC-BUD-008-1` 予算超過後も認可・入力条件を満たす取引を登録・編集でき、実績と超過額が更新される。
- `AC-BUD-009-1` 古いversionによる改定の更新、存在しない改定への更新、既に存在する月への新規作成を競合として拒否する。画面で新しい改定versionを受け取った場合は入力値も同じ改定へ更新し、古い入力と新しいversionによる上書きを防ぐ。
- `AC-BUD-010-1` 予算画面と分析概要は同じ純関数で予算進捗を作り、同じ月の予算・実績・残額・消化率・状態が一致する。分析概要はグループ対象のときだけ予算カードを表示する。
- `AC-BUD-010-2` 320px、375 x 812、1280 x 800で金額・状態・主要操作に横スクロールや重なりがなく、カテゴリ内訳は縦一覧で表示される。
- `AC-BUD-010-3` 予算フォームは入力中にカテゴリ予算の合計と未配分額を表示する。合計がグループ予算を超えるときは送信を無効化し、サーバーでも拒否する。月・グループの切替時は遷移先の予算で入力と停止確認を初期化し、実績だけの更新では入力とfocusを維持する。
- `AC-BUD-010-4` 予算フォームの金額欄（グループ予算・各カテゴリ予算）は`inputmode="none"`としてOSの仮想キーボードを開かず、取引入力と同じ画面内テンキーの1〜9、0、00で入力し、1文字削除できる。テンキーは金額欄へfocusまたはタップしたときにその欄の直下へ開き、キー操作は選択中の欄だけへ反映する。別の金額欄を選ぶと移動し、金額欄以外へfocusが移ると閉じる。閉じている間は再び開けることを画面上の説明で示し、物理キーボードとスクリーンリーダーからの入力は開閉状態にかかわらず維持する。
- `AC-BUD-010-5` 予算フォームの金額欄（グループ予算・各カテゴリ予算）は入力中も取引入力（`AC-TXN-014-10`）と同じ3桁区切りで表示する（`300000`と打つと`300,000`。適用改定からの初期値も同じ）。表示用の`input`は`name`を持たず、送信する`totalAmountMinor`と`categoryLimit:<categoryId>`は区切りなしの整数のままhidden inputで送る。合計・未配分額の計算は区切りなしの状態から行う。物理キーボードや貼り付けで入った`,`は無視し、区切りはlocale実装に依存しない決定的な整形で行う。

## UC-021 LINEグループへ週次レポートを受け取る

操作者: なし（Cloud Schedulerが起動する定期ジョブ）。連携の登録・解除はLINEグループへのbot招待・退出で行う。

基本フロー:

1. owner/adminがLINE公式アカウント（bot）を家族のLINEグループへ招待する。join eventがWebhookで届き、対象の家計グループとの連携が登録される。
2. 毎週日曜21:00（Asia/Tokyo）にCloud Schedulerが週次ジョブを呼ぶ。ジョブはOIDCトークンを検証し、連携先を確認する。
3. ジョブは通知専用ロールで対象月と前月の支出・収入の最小列（支払者・負担額）、固定費の展開条件、対象月の適用予算改定、アクティブメンバーの表示名を読み、画面と同じ純関数で対象週の支出とメンバーの負担額、対象月の累計と先月の同時点、月末の見込み、予算進捗を計算する。
4. 送信枠を確保してからLINEグループへテキスト1通をpushする。送信に失敗した場合は枠を返上し、Cloud Schedulerのリトライに委ねる。
5. botをグループから退出させると連携が解除され、以後の週次ジョブは送信せずに終了する。

受け入れ条件:

- `AC-NOTIF-001-1` 対象週の算出は、グループのタイムゾーン上の送信日を含む直近の月曜〜日曜を返し、月初・年末年始の境界、UTCと暦日がずれる時刻、月曜以降のリトライで正しい。
- `AC-NOTIF-001-2` 対象月は対象週の日曜が属する月とし、読み込む月は対象月とその前月の2か月とする。累計の期間は対象月1日〜対象週の日曜、経過日数はその日数、先月の同時点は前月1日〜同じ経過日数（前月の日数を上限）になる。
- `AC-NOTIF-002-1` 通知文面は「【グループ名】週次サマリー」の後に、今週（支出合計と先週比を1行、カテゴリ上位5件と「その他のカテゴリ」の各行に前週の同じカテゴリとの差額、0円の行なし）、メンバーの支出（負担額と構成比、金額降順）、対象月の累計（経過日数、支出・収入・収支、先月の同時点と差額、月末の見込み）、メンバーの累計支出、予算がある対象月だけ予算ブロック（予算額、10段階の文字バーと消化率と残額または超過額、1日あたり使える額、見込みの予算内・超過判定、注意・超過のカテゴリ予算）を仕様の形式で組み立てる。対象週の支出が0件の週は今週ブロックを0件用文面にしメンバーの支出を省く。予算がない月は予算ブロックなし。金額に浮動小数点を使わない。
- `AC-NOTIF-002-3` 消化率の文字バーは`█`を消化率10%ごとに1つ（上限10個）、残りを`░`で埋めた10文字になる。1日あたり使える額は残額を残り日数で割った整数（切り捨て）で、残額が0円以下または残り日数0日のときは行を出さない。見込み判定は月末の見込みが予算額以下なら「予算内」、超えるなら「予算超過」になる。
- `AC-NOTIF-002-2` 通知文面にはアクティブメンバーの表示名と負担額を含めるが、支払額、件数、メモ、取引の名称、個別明細は含めない。通知用DB関数の返却値にメモと取引の名称が含まれない。
- `AC-NOTIF-003-1` 対象週・累計・先月の同時点の支出・収入・収支・カテゴリ別支出は、分析モジュールが公開する集計純関数へ同じ入力を渡して計算し、固定費の展開結果（支払者・負担額を含む）を含み、削除済み取引と収入を支出から除外する。メンバーの支出は同じ純関数のメンバー対象で求めた負担額で、グループ支出との構成比は整数パーセントになる。対象月全体のグループ集計は概要分析と同じ値になる。
- `AC-NOTIF-003-3` 月末の見込みは「累計支出 ＋ 四捨五入(累計の変動費 × 残り日数 ÷ 経過日数) ＋ 未到来の固定費」を整数演算で求め、残り日数0日では累計支出と一致する。変動費は単発取引、固定費は展開した擬似取引で区別する。
- `AC-NOTIF-003-2` 予算進捗は予算モジュールが公開する`calculateBudgetProgress`へ対象月の集計を渡して計算し、予算画面と同じ予算額・実績・残額・消化率・状態になる。停止改定・適用改定なしの月は予算ブロックを出さない。
- `AC-NOTIF-004-1` join eventで連携が登録され、leave eventで解除される。group以外のsource（1対1トーク等）と不正な形式のgroupIdは登録しない。
- `AC-NOTIF-005-1` 署名が不正・欠落したWebhook要求は本文を処理せず、署名検証は定数時間比較で行う。
- `AC-NOTIF-006-1` OIDCトークンが不正・欠落・audience不一致・service account不一致のジョブ要求は実行しない。
- `AC-NOTIF-007-1` 通知専用ロールが、許可された関数以外（テーブル直接参照、他機能のDB関数を含む）へアクセスできないことを統合テストで証明する。集計元の関数は支出・固定費の行に支払者と負担額、アクティブメンバーの識別子と表示名を返し、メモと取引の名称を返さない。
- `AC-NOTIF-008-1` 同一グループ・同一週の2回目の送信要求は、LINEへのpushを行わず成功応答で終わる。送信失敗後は枠が返上され、再試行で送信できる。
- `AC-NOTIF-010-1` 環境変数未設定時に両エンドポイントが404を返し、他機能のテストへ影響しない。未連携のグループでは集計元を読まずに送信せず終了する。

## UC-022 グループ設定を変更する

操作者: ownerまたはadmin。memberは閲覧だけできる。

基本フロー:

1. ownerまたはadminが設定画面の「グループ設定」でグループ名、週の開始曜日、標準の分け方を編集して保存する。
2. Server Actionがbind引数のgroupID、入力値、読み込み時の`version`をschema検証する。
3. DB関数が検証済みsessionの許可リストと対象グループのowner/admin所属を再確認し、グループ行をlockして`version`を比較する。
4. 一致すれば名称・週の開始曜日・標準負担方法を更新して`version`を加算し、不一致なら競合として返す。
5. 画面は結果を同じ項目内へ表示し、成功時は画面見出しのグループ名と項目内の現在値を更新後の値にする。

受け入れ条件:

- `AC-GRP-013-1` owner/adminには設定画面の「グループ設定」項目にグループ名、週の開始曜日、標準の分け方の編集フォームと保存操作を表示する。memberには同じ項目を読み取り専用で表示し、編集操作を提供しない。memberや非メンバーがフォームを偽造して送信しても更新できない。
- `AC-GRP-013-2` 通貨とタイムゾーンは表示のみとし、変更手段を提供しない。MVPではJPY・`Asia/Tokyo`固定である旨を項目内の補足文で示す。DB関数は通貨・タイムゾーンを更新しない。
- `AC-GRP-013-3` グループ名は前後空白を除いて1〜50文字、週の開始曜日は日曜（0）・月曜（1）、標準の分け方は「均等（`equal`）」「自分（`self`）」だけを受け付け、Server Actionはschema検証でそれ以外を拒否してフィールド単位のエラーを表示する。クライアントから渡されたgroupID、権限、`version`を信用せず、DB関数で再確認する。
- `AC-GRP-013-4` 更新は`security definer`のDB関数`update_group_settings`だけで行い、`authenticated`へ`groups`の直接update権限を与えない。関数は`search_path`を空文字へ固定し、検証済みGoogle sessionの許可リストと対象グループのowner/admin所属を再確認して、member、非メンバー（別グループのownerを含む）、存在しないグループを同じ権限エラーで拒否し、グループの存在を明かさない。
- `AC-GRP-013-5` `groups.version`による楽観的ロックを適用する。読み込み時の`version`と一致しない更新は上書きせず競合として返し、画面は他のメンバーが先に変更した旨と再読み込みを案内して入力値を破棄しない。現在の値と同じ内容の保存は更新も`version`の加算も行わず成功として返す（再送の冪等性）。
- `AC-GRP-013-6` 成功時は他画面へ遷移せず、同じ項目内へ結果を表示し、画面見出しのグループ名、項目内の現在値、フォームが保持する`version`を更新後の値にする。グループ名はホーム、履歴、グループ一覧など他画面でも次回表示から反映される。
- `AC-GRP-013-7` 標準の分け方の変更は既存取引の内訳を変更せず、以後の新規入力の初期値だけに影響する。週の開始曜日の変更はカレンダーの次回表示から反映され、保存済みの取引日を変更しない。

## 認可マトリクス

| 操作                    | owner | admin | member | 非メンバー |
| ----------------------- | ----: | ----: | -----: | ---------: |
| グループ取引の閲覧      |    可 |    可 |     可 |       不可 |
| 取引作成                |    可 |    可 |     可 |       不可 |
| 取引編集・削除          |    可 |    可 |     可 |       不可 |
| カテゴリ管理            |    可 |    可 |   不可 |       不可 |
| メンバー招待            |    可 |    可 |   不可 |       不可 |
| メンバー権限変更        |    可 |  不可 |   不可 |       不可 |
| owner以外のメンバー削除 |    可 |  不可 |   不可 |       不可 |
| グループ設定の変更      |    可 |    可 |   不可 |       不可 |
| CSV出力                 |    可 |    可 |     可 |       不可 |
| 固定費の閲覧            |    可 |    可 |     可 |       不可 |
| 固定費の設定            |    可 |    可 |   不可 |       不可 |
| 概要分析の閲覧          |    可 |    可 |     可 |       不可 |
| 詳細分析の閲覧          |    可 |    可 |     可 |       不可 |
| 予算・実績の閲覧        |    可 |    可 |     可 |       不可 |
| 予算の設定・改定・停止  |    可 |    可 |   不可 |       不可 |
| LINE週次レポートの受信  |    可 |    可 |     可 |       不可 |

LINE週次レポートは連携済みのLINEグループトークへ届くため、受信者はLINEグループの参加者で決まる。botの招待はowner/adminが行い、アプリ側の権限では制御しない。

# CLAUDE.md

本リポジトリのエージェント・開発者向けルールの正本は `AGENTS.md` である。

作業を開始する前に必ず `AGENTS.md` を読み、記載されたすべてのルールに従うこと。特に次を厳守する。

- 最優先の開発手順(仕様作成 → レビュー → テスト → 実装 → 検証)
- 作業ごとに専用のGit worktreeとブランチを作成する運用
- エージェントはPRのmergeをいかなる場合も絶対に行わず、作業はPRの作成・更新までとする

@AGENTS.md
